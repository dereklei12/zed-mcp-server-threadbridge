ThreadBridge is a persistent memory server for AI assistants. It automatically downloads the appropriate binary for your platform.

No additional configuration is required. The extension will download and manage the ThreadBridge binary automatically.

Optionally, you can set a `project_path` to specify which project's memory to use.
